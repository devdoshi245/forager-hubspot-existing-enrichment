# This project is a one-off CLI job, NOT a web service. There is intentionally no
# web/worker process so Railway will not auto-run anything (which could spend credits).
# Run it explicitly, e.g.:  railway run python -m bulk.cli estimate
